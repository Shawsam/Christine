<view class="container">
    <view class="top">
      <image class="card1" src="../../image/cardbind.jpg" />
   </view>
	<view class="main">
		<view class="tip">卡片绑定成功</view>
		<view>卡号尾号为<span class="number">{{subcardNo}}</span>的卡，已放入您的卡包</view>
	    <image class="card" src="{{cardImg}}" />
	</view>
	<view class="flexbox btnGroup">
		<view class="flex-item btn" bindtap="ToCard">前往卡包</view>
		<view class="flex-item btn" bindtap="ToConsume">立即消费</view>
	</view>
</view>
